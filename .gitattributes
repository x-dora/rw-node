# 本仓库的脚本在 Linux 容器 / PaaS 中以 bash 执行，文本文件行尾必须是 LF。
# 若 start.sh 带上 CRLF，bash 会把第 2 行的选项名读成 "pipefail\r"，
# 报 `set: pipefail: invalid option name` 并以退出码 2 终止启动。
# .gitattributes 优先级高于 core.autocrlf，Windows 工作区检出时同样会落成 LF。
* text=auto eol=lf
